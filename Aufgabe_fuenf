def find_max(numbers):
    maximum = numbers[0]
    for number in numbers:
        if number > maximum:
            maximum = number
    return maximum
zahlen = [3, 7, 2, 9, 5, 12, 4]
groesstes_element = find_max(zahlen)
print("Das größte Element ist:", groesstes_element)